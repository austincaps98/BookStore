@{
}

@model IEnumerable<string>

<div class="d-grid gap-2">
    <a asp-controller="Home" asp-action="Index" asp-route-bookCategory="Marriage" asp-route-pageNum ="1"
       class="btn btn-block btn-primary">Marriage</a>

    @*@foreach (string t in Model)
    {
        <a asp-controller="Home" asp-action="Index" asp-route-bookCategory="@t" asp-route-pageNum ="1"
           class="btn btn-block @(ViewBag.SelectedType == t ? "btn-primary" : "btn-outline-secondary")" style="white-space:pre-line">@t</a>
    }*@
</div>
